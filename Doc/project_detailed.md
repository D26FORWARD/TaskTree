# SplitMind - 企业级多智能体AI开发平台详细文档

![Version](https://img.shields.io/badge/Version-2.0-electric--cyan)
![Platform](https://img.shields.io/badge/Platform-Web%20Dashboard-blue)
![AI](https://img.shields.io/badge/AI-Claude%20Powered-purple)

---

## 📋 目录

1. [项目概述](#项目概述)
2. [问题与解决方案](#问题与解决方案)
3. [目标用户](#目标用户)
4. [核心特性](#核心特性)
5. [技术架构](#技术架构)
6. [系统设计](#系统设计)
7. [工作流程](#工作流程)
8. [项目结构](#项目结构)
9. [主要组件](#主要组件)
10. [创新亮点](#创新亮点)
11. [应用场景](#应用场景)
12. [竞争优势](#竞争优势)
13. [技术栈](#技术栈)

---

## 项目概述

### 什么是 SplitMind？

**SplitMind** 是一个革命性的企业级多智能体AI开发平台，它通过协调多个AI代理（Agents）并行工作，实现了软件开发任务的智能化自动化。这是一个将人工智能与软件工程深度融合的创新平台，让AI不仅仅是辅助工具，而是能够真正"思考"和"协作"的开发伙伴。

### 核心理念

SplitMind 的核心理念是**"AI智能体编排"**——就像一个优秀的项目经理协调多个开发人员一样，SplitMind 能够：
- 🧠 **智能规划**：将复杂的软件项目分解为可管理的任务
- 🤖 **并行执行**：启动多个AI代理同时处理不同任务
- 🔗 **协同合作**：确保AI代理之间相互协调，避免冲突
- 📊 **实时监控**：提供可视化的项目进度和代理状态监控
- 🔄 **自动集成**：自动合并完成的工作到主分支

### 版本历程

SplitMind v2.0 是一个里程碑式的重大升级：
- **v1.x**：基础的命令行工具，支持基本的多代理任务分配
- **v2.0**：完全重构为现代化Web应用，引入A2AMCP协议，实现企业级功能

---

## 问题与解决方案

### 软件开发中的痛点

#### 1. **开发效率瓶颈**
**问题**：传统开发流程中，一个开发人员同一时间只能处理一个任务，大型项目需要数周甚至数月才能完成。

**SplitMind的解决方案**：
- 启动多个AI代理并行处理不同任务
- 一个5人天的项目可以压缩到几个小时完成
- 代理24/7不间断工作，没有疲劳或休息需求

#### 2. **团队协作复杂性**
**问题**：多人协作开发时容易出现代码冲突、重复工作、沟通成本高等问题。

**SplitMind的解决方案**：
- **A2AMCP协议**：实现AI代理之间的智能协调
- **文件锁机制**：自动防止代码冲突
- **消息通信**：代理间可以相互查询和分享信息
- **接口共享**：统一的类型定义和组件规范

#### 3. **任务规划困难**
**问题**：将一个大型功能分解为合理的开发任务需要丰富的经验和大量时间。

**SplitMind的解决方案**：
- **AI Task Master**：利用Claude AI自动生成详细的项目计划
- **波次管理**：智能识别任务依赖关系，按波次执行
- **动态调整**：根据执行情况自动调整任务优先级

#### 4. **质量保证挑战**
**问题**：快速开发往往以牺牲代码质量为代价。

**SplitMind的解决方案**：
- **结构化提示**：为每个AI代理提供明确的编码规范和质量标准
- **代码审查**：支持merge前的自动化审查流程
- **测试集成**：任务可以包含单元测试和集成测试要求

#### 5. **项目管理复杂**
**问题**：跟踪多个并行任务、协调资源、监控进度需要大量管理工作。

**SplitMind的解决方案**：
- **Web Dashboard**：直观的可视化项目管理界面
- **实时更新**：WebSocket驱动的即时状态同步
- **多项目支持**：在单一界面管理多个项目
- **自动化编排**：Orchestrator自动管理任务队列和代理生命周期

---

## 目标用户

### 主要用户群体

#### 1. **独立开发者 & 创业者**
**场景**：
- 需要快速构建MVP（最小可行产品）
- 资源有限，无法组建完整开发团队
- 想要在有限时间内实现更多功能

**价值**：
- 将开发效率提升5-10倍
- 降低早期开发成本
- 快速验证产品想法

#### 2. **中小型开发团队**
**场景**：
- 项目截止日期紧张
- 需要并行开发多个功能模块
- 希望提升团队整体产出

**价值**：
- 补充团队开发能力
- 处理繁琐的重复性开发任务
- 让人类开发者专注于核心架构和复杂逻辑

#### 3. **企业开发团队**
**场景**：
- 维护多个遗留项目
- 需要快速实现新功能
- 处理大量技术债务

**价值**：
- 企业级安全和管理功能
- 项目标准化和流程自动化
- 审计追踪和合规支持

#### 4. **技术顾问 & 外包团队**
**场景**：
- 同时服务多个客户
- 需要快速交付定制化功能
- 维护多个不同技术栈的项目

**价值**：
- 提升项目吞吐量
- 降低人力成本
- 保持高质量的交付标准

#### 5. **开源项目维护者**
**场景**：
- 处理大量issue和feature request
- 维护文档和测试
- 社区贡献代码的审查和集成

**价值**：
- 自动化routine工作
- 快速实现社区需求的功能
- 保持项目活跃度

### 用户技能要求

**基础要求**：
- 了解Git版本控制
- 基本的软件开发概念
- 能够编写清晰的任务描述

**进阶使用**：
- 了解项目架构设计
- 能够编写有效的AI提示词
- 理解代码审查和质量标准

**企业部署**：
- 系统管理和DevOps经验
- 安全最佳实践知识
- 容器化和微服务架构经验

---

## 核心特性

### 1. 🎛️ 现代化Web Dashboard

**特点**：
- **响应式设计**：适配桌面、平板和移动设备
- **暗色主题**：减少眼睛疲劳的专业界面
- **实时更新**：WebSocket驱动，无需刷新页面
- **直观操作**：拖拽式任务管理，点击即用

**主要界面**：
- **项目选择器**：快速切换不同项目
- **任务看板**：可视化任务状态流转
- **代理监控**：实时查看AI代理工作状态
- **协调中心**：监控多代理协作情况
- **项目管理器**：集中管理所有项目

### 2. 🧠 AI驱动的任务规划（AI Task Master）

**智能规划引擎**：
```
用户输入：项目概述 + 初始需求
    ↓
 AI分析
    ↓
生成结构化开发计划
    ↓
自动创建任务列表
    ↓
识别依赖关系
    ↓
设置优先级和波次
```

**生成的内容**：
- **详细的任务描述**：包含上下文和技术要求
- **依赖关系图**：明确哪些任务必须先完成
- **波次分组**：第一波是基础设施，后续波次逐步构建功能
- **自定义提示词**：为每个任务定制AI代理的指令

**示例**：对于一个电商网站项目
```
波次1（基础设施）:
- 设置项目结构和依赖
- 配置数据库schema
- 实现用户认证系统

波次2（核心功能）:
- 构建产品目录API
- 实现购物车功能
- 创建订单管理系统

波次3（增强功能）:
- 集成支付网关
- 添加产品搜索
- 实现评论系统
```

### 3. 🤖 多代理并行执行

**工作机制**：

1. **任务队列管理**：
   - `UNCLAIMED`：未认领的任务
   - `UP_NEXT`：准备执行的任务队列
   - `IN_PROGRESS`：正在执行的任务
   - `COMPLETED`：已完成待合并
   - `MERGED`：已合并到主分支

2. **智能调度**：
   - 根据系统资源和配置自动决定启动多少代理
   - 优先级高的任务优先执行
   - 检查任务依赖，确保前置任务完成

3. **隔离执行环境**：
   - 每个代理工作在独立的Git worktree
   - 独立的分支，避免相互干扰
   - 专属的tmux session，可独立监控

**代理生命周期**：
```
启动 → 环境初始化 → 执行任务 → 提交代码 → 标记完成 → 清理资源
```

### 4. 🔗 A2AMCP协调协议

**Agent-to-Agent Model Context Protocol（代理间模型上下文协议）**

这是SplitMind的核心创新之一，解决了多个AI代理并行工作时的协调问题。

#### 协议组件：

**1. 代理注册**：
```python
register_agent(project_id, session_name, task_id, branch, task_title)
```
- 代理启动时向协调中心报告
- 记录代理的身份和职责
- 设置心跳监控

**2. 文件锁定**：
```python
announce_file_change(project_id, session_name, file_path, action, reason)
```
- 代理在修改文件前声明意图
- 其他代理看到锁定信息，避免冲突
- 完成后释放锁定

**3. 接口共享**：
```python
register_interface(project_id, session_name, name, definition)
query_interface(project_id, interface_name)
```
- 代理A创建一个TypeScript接口
- 代理B可以查询并使用相同的接口定义
- 确保类型一致性

**4. 代理间通信**：
```python
query_agent(project_id, from_session, to_session, topic, message)
broadcast_message(project_id, session_name, topic, message)
```
- 直接向特定代理提问
- 广播重要信息给所有代理

**5. 任务追踪**：
```python
add_todo(project_id, session_name, title, order)
update_todo(project_id, session_name, todo_id, status)
```
- 代理将任务分解为todos
- 实时更新进度
- 其他代理可查看进度

#### 技术实现：
- **存储**：Redis作为协调数据存储
- **通信**：MCP (Model Context Protocol) 服务器
- **SDK**：Python客户端库，封装所有协调功能
- **Docker**：容器化部署，简化环境配置

### 5. 📊 实时监控与可视化

#### 协调中心Dashboard：

**1. 活跃代理图**：
```
    Agent-1 ←→ Redis ←→ Agent-2
       ↓                    ↓
    [File Lock]        [Todo List]
```
- 节点：代表每个活跃的AI代理
- 连接线：显示代理间的通信
- 颜色编码：绿色=活跃，黄色=工作中，红色=离线

**2. 事件流**：
实时显示所有协调事件：
```
[10:23:45] Agent task-001 registered
[10:23:46] Agent task-001 locked file: src/components/Button.tsx
[10:24:12] Agent task-002 queried interface: ButtonProps
[10:24:15] Agent task-001 released lock: src/components/Button.tsx
```

**3. 统计面板**：
- 活跃代理数量
- Todo完成率
- 文件锁定数量
- 代理间消息数量

#### 代理监控：

**代理卡片**：
```
┌─────────────────────────────┐
│ Agent: task-001-myproject   │
│ Task: Implement user auth   │
│ Status: 🟢 Running          │
│ Started: 10:23:45           │
│ Duration: 05:23             │
│ Progress: ████████░░ 80%    │
│ [View in iTerm] [Stop]      │
└─────────────────────────────┘
```

### 6. 🛡️ 企业级安全与管理

#### 项目删除保护：
```
⚠️ 警告：此操作不可撤销
请输入项目名称以确认: [_________]
□ 完整清理（删除worktrees、分支、会话等）
[删除] ← 输入正确项目名前保持禁用
```

#### 项目重置：
- 保留项目配置
- 清除所有任务和代理
- 重置Git状态到clean state
- 清理临时文件

#### API密钥管理：
- 安全存储在本地config.json
- 支持多个API提供商
- 密钥验证机制

#### 审计日志：
- 记录所有关键操作
- 时间戳和操作者信息
- 便于问题追踪和合规审查

### 7. ⚙️ 灵活的配置系统

#### 全局配置：
```json
{
  "orchestrator": {
    "max_concurrent_agents": 5,
    "auto_merge": false,
    "auto_spawn_interval": 60,
    "merge_strategy": "merge"
  },
  "api": {
    "provider": "anthropic",
    "model": "claude-sonnet-4-20250514",
    "api_key": "***"
  }
}
```

#### 项目配置：
```json
{
  "project_id": "my-ecommerce",
  "name": "E-commerce Platform",
  "max_agents": 3,
  "project_overview": "...",
  "initial_prompt": "..."
}
```

#### 任务配置：
```json
{
  "task_id": "task-001",
  "title": "Implement user authentication",
  "dependencies": [],
  "priority": 1,
  "merge_order": 1,
  "custom_prompt": "Use JWT for authentication..."
}
```

---

## 技术架构

### 系统架构图

```
┌───────────────────────────────────────────────────────────────┐
│                        Web Browser                             │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │         React Dashboard (TypeScript)                    │  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │  │
│  │  │ Project  │ │   Task   │ │  Agent   │ │  Coord   │   │  │
│  │  │ Manager  │ │  Board   │ │ Monitor  │ │ Center   │   │  │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘   │  │
│  └─────────────────────────────────────────────────────────┘  │
└───────────────────────────────┬───────────────────────────────┘
                                │ HTTP / WebSocket
                                ↓
┌───────────────────────────────────────────────────────────────┐
│                   FastAPI Backend (Python)                     │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  API Routes  │  WebSocket Manager  │  Orchestrator     │  │
│  ├─────────────────────────────────────────────────────────┤  │
│  │  Project Manager  │  Task Manager  │  Config Manager   │  │
│  ├─────────────────────────────────────────────────────────┤  │
│  │  A2AMCP Integration  │  Merge Queue  │  Coordination   │  │
│  └─────────────────────────────────────────────────────────┘  │
└───────────────┬───────────────────┬───────────────────────────┘
                │                   │
                ↓                   ↓
┌───────────────────────┐  ┌───────────────────────┐
│   File System         │  │   Redis               │
│  ┌─────────────────┐  │  │  ┌─────────────────┐  │
│  │ config.json     │  │  │  │ Agent Registry  │  │
│  │ projects.json   │  │  │  │ File Locks      │  │
│  │ worktrees/      │  │  │  │ Todos           │  │
│  │ .splitmind/     │  │  │  │ Messages        │  │
│  └─────────────────┘  │  │  │ Interfaces      │  │
└───────────────────────┘  │  └─────────────────┘  │
                           └───────────────────────┘
                                    ↑
                                    │ A2AMCP Protocol
                                    ↓
┌───────────────────────────────────────────────────────────────┐
│                     AI Agents (Claude Code)                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │  Agent-1     │  │  Agent-2     │  │  Agent-N     │        │
│  │ [task-001]   │  │ [task-002]   │  │ [task-nnn]   │        │
│  │              │  │              │  │              │        │
│  │ tmux session │  │ tmux session │  │ tmux session │        │
│  │ git worktree │  │ git worktree │  │ git worktree │        │
│  └──────────────┘  └──────────────┘  └──────────────┘        │
└───────────────────────────────────────────────────────────────┘
```

### 数据流

#### 1. 任务创建流程：
```
用户输入 → AI Task Master → 任务列表生成 → 存储到projects.json
    ↓
Orchestrator 扫描
    ↓
任务状态: UNCLAIMED → UP_NEXT → 代理队列
```

#### 2. 代理执行流程：
```
Orchestrator检测到UP_NEXT任务
    ↓
创建Git worktree
    ↓
启动tmux session
    ↓
启动Claude Code代理
    ↓
代理向Redis注册 (A2AMCP)
    ↓
执行任务 + 实时更新协调信息
    ↓
提交代码到分支
    ↓
标记任务COMPLETED
    ↓
进入合并队列
```

#### 3. 实时更新流程：
```
Backend事件发生 (任务更新/代理状态变化)
    ↓
生成WebSocketMessage
    ↓
WebSocketManager广播
    ↓
所有连接的客户端接收
    ↓
前端React组件自动重新渲染
```

### 关键技术决策

#### 为什么选择FastAPI？
- **高性能**：基于Starlette和Pydantic，异步支持
- **自动文档**：OpenAPI和Swagger UI自动生成
- **类型安全**：Python类型提示的完整支持
- **WebSocket支持**：原生支持实时通信

#### 为什么选择React？
- **组件化**：易于构建复杂的UI
- **生态系统**：丰富的第三方库支持
- **性能**：虚拟DOM和高效的渲染
- **TypeScript**：类型安全减少bug

#### 为什么选择Redis？
- **速度**：内存数据库，毫秒级响应
- **数据结构**：丰富的数据类型支持协调需求
- **Pub/Sub**：支持实时消息广播
- **持久化**：可选的数据持久化机制

#### 为什么选择tmux？
- **会话管理**：持久化的终端会话
- **远程访问**：可以attach查看代理工作
- **进程隔离**：每个代理独立运行
- **跨平台**：Linux和macOS原生支持

---

## 系统设计

### 核心模块

#### 1. Project Manager（项目管理器）
**职责**：
- 创建、更新、删除项目
- 管理项目配置和元数据
- 初始化项目目录结构
- Git仓库验证和设置

**关键文件**：
- `dashboard/backend/project_manager.py`
- `dashboard/backend/config.py`

**数据模型**：
```python
class Project(BaseModel):
    id: str
    name: str
    path: str
    description: Optional[str]
    project_overview: Optional[str]
    initial_prompt: Optional[str]
    plan: Optional[str]
    max_agents: int = 5
    created_at: datetime
```

#### 2. Orchestrator（编排器）
**职责**：
- 管理任务队列（UNCLAIMED → UP_NEXT → IN_PROGRESS）
- 决定何时启动新代理
- 监控代理状态
- 触发自动合并

**关键文件**：
- `dashboard/backend/orchestrator.py`
- `dashboard/backend/a2amcp_orchestrator.py`

**核心循环**：
```python
async def _orchestrator_loop():
    while running:
        # 1. 管理任务队列
        await _manage_task_queue()
        
        # 2. 启动代理
        await _spawn_agents()
        
        # 3. 检查代理状态
        await _check_agent_status()
        
        # 4. 处理完成的任务
        await _check_and_merge_completed_tasks()
        
        # 5. 等待下一个周期
        await asyncio.sleep(interval)
```

#### 3. A2AMCP Coordination（协调系统）
**职责**：
- 维护代理注册表
- 管理文件锁
- 处理代理间消息
- 追踪任务进度（todos）
- 共享接口定义

**关键文件**：
- `dashboard/backend/coordination_monitor.py`
- `dashboard/backend/a2amcp_merge_queue.py`

**Redis数据结构**：
```
splitmind:{project_id}:agents -> Hash (代理注册信息)
splitmind:{project_id}:heartbeat -> Hash (代理心跳)
splitmind:{project_id}:todos -> Hash (任务追踪)
splitmind:{project_id}:file_locks -> Hash (文件锁)
splitmind:{project_id}:interfaces -> Hash (接口定义)
splitmind:{project_id}:messages -> List (消息队列)
```

#### 4. WebSocket Manager（实时通信管理器）
**职责**：
- 管理WebSocket连接
- 广播事件到所有客户端
- 处理客户端订阅
- 连接断开和重连处理

**关键文件**：
- `dashboard/backend/websocket_manager.py`

**消息类型**：
```python
class WebSocketMessage(BaseModel):
    type: str  # 消息类型
    project_id: Optional[str]  # 项目ID
    data: Dict[str, Any]  # 数据payload
    timestamp: datetime  # 时间戳
```

**消息类型**：
- `task_status_changed` - 任务状态更新
- `agent_spawned` - 代理启动
- `agent_completed` - 代理完成
- `orchestrator_started` - 编排器启动
- `orchestrator_stopped` - 编排器停止
- `coordination_update` - 协调信息更新

#### 5. Frontend Components（前端组件）

**主要组件**：
```
App.tsx (主应用)
  ├── ProjectSelector (项目选择器)
  ├── CommandCenter (命令中心)
  │   ├── TaskBoard (任务看板)
  │   ├── AgentMonitor (代理监控)
  │   ├── AgentCoordination (协调中心)
  │   └── OrchestratorControl (编排器控制)
  ├── ProjectManager (项目管理器)
  ├── GlobalSettings (全局设置)
  └── HelpCenter (帮助中心)
```

### 状态管理

#### 前端状态：
- **React Query**：服务器状态缓存和同步
- **React State**：本地UI状态
- **WebSocket**：实时状态更新

#### 后端状态：
- **内存**：运行时状态（Orchestrator配置）
- **文件系统**：持久化配置（config.json, projects.json）
- **Redis**：协调状态（代理信息、锁、消息）

### 安全性设计

#### API安全：
- **CORS配置**：限制允许的origin
- **输入验证**：Pydantic模型验证所有输入
- **路径验证**：防止路径遍历攻击
- **错误处理**：不暴露内部错误细节

#### 数据安全：
- **本地存储**：敏感数据（API密钥）存储在本地
- **权限控制**：文件系统权限保护配置文件
- **清理机制**：安全删除临时文件和会话

#### Git安全：
- **分支隔离**：每个任务独立分支
- **Worktree**：物理隔离的工作目录
- **自动清理**：完成后清理worktree
- **合并审查**：可选的人工审查流程

---

## 工作流程

### 完整的开发周期

#### 第一步：初始化项目

```bash
# 1. 安装SplitMind
git clone https://github.com/webdevtodayjason/splitmind.git
cd splitmind
python setup.py

# 2. 启动Dashboard
python launch-dashboard.py
```

浏览器自动打开 http://localhost:8000

#### 第二步：配置全局设置

1. 点击右上角⚙️图标
2. 输入Anthropic API密钥
3. 配置Orchestrator参数：
   - 最大并发代理数：5
   - 自动合并：开启/关闭
   - 启动间隔：60秒

#### 第三步：创建项目

**方式A：通过UI创建**
1. 点击"创建项目"按钮
2. 填写项目信息：
   ```
   项目名称: my-ecommerce
   描述: 一个现代化的电商平台
   Git仓库路径: /path/to/my-ecommerce
   最大代理数: 3
   ```
3. 点击"创建"

**方式B：导入现有项目**
1. 选择现有Git仓库路径
2. SplitMind自动检测项目信息
3. 确认并创建

#### 第四步：AI驱动的任务规划

1. 进入项目详情页
2. 切换到"Settings"标签
3. 填写项目概述：
   ```
   这是一个B2C电商平台，主要功能包括：
   - 用户注册和登录
   - 产品浏览和搜索
   - 购物车管理
   - 订单处理
   - 支付集成（Stripe）
   ```
4. 填写初始提示：
   ```
   使用React和TypeScript构建前端
   使用Node.js和Express构建后端
   使用PostgreSQL数据库
   遵循RESTful API设计原则
   ```
5. 点击"🚀 AI Task Master"
6. 等待AI生成完整的开发计划（约30秒）
7. 审查生成的任务列表
8. （可选）手动编辑或调整任务

生成的任务示例：
```
波次 1（基础）:
├── task-001: 项目结构初始化
├── task-002: 配置TypeScript和构建工具
├── task-003: 设置数据库schema
└── task-004: 实现用户认证API

波次 2（核心功能）:
├── task-005: 构建产品目录API
├── task-006: 实现购物车功能
└── task-007: 创建订单管理系统

波次 3（集成）:
├── task-008: Stripe支付集成
├── task-009: 产品搜索实现
└── task-010: 前端UI组件开发
```

#### 第五步：启动自动化开发

1. 回到项目主页
2. 点击侧边栏的"启动Orchestrator"
3. Orchestrator自动开始工作：

**自动化流程**：
```
10:00:00 - Orchestrator启动
10:00:01 - 任务队列管理：
           UNCLAIMED (10) → UP_NEXT (3)
10:00:02 - 启动Agent-1处理task-001
           创建worktree: worktrees/task-001-init-structure
           创建分支: task-001-init-structure
           启动tmux session: task-001-my-ecommerce
10:00:03 - Agent-1注册到协调中心
10:00:05 - 启动Agent-2处理task-002
10:00:06 - 启动Agent-3处理task-003
10:00:10 - Agent-1工作中：创建项目结构
10:00:15 - Agent-2工作中：配置TypeScript
10:00:20 - Agent-3工作中：设计数据库schema
10:15:00 - Agent-1完成task-001
           提交代码：3个文件更改
           状态更新：COMPLETED
           进入合并队列
10:15:30 - 合并task-001到main分支
           清理worktree
           任务状态：MERGED
10:15:31 - 启动Agent-4处理task-004
           （task-001的依赖已满足）
```

#### 第六步：实时监控

**任务看板**：
```
┌─────────────┬─────────────┬─────────────┬─────────────┐
│  UNCLAIMED  │   UP_NEXT   │IN_PROGRESS  │  COMPLETED  │
├─────────────┼─────────────┼─────────────┼─────────────┤
│ task-008 ■  │ task-005 □  │ task-001 ▶  │ task-004 ✓  │
│ task-009 ■  │ task-006 □  │ task-002 ▶  │             │
│ task-010 ■  │ task-007 □  │ task-003 ▶  │             │
└─────────────┴─────────────┴─────────────┴─────────────┘
```

**代理监控**：
```
Agent-1 [task-001-my-ecommerce]
🟢 运行中 | 开始时间: 10:00:02 | 持续时间: 15:23
进度: ████████████████░░░░ 80%
最后活动: 修改 src/server.ts

Agent-2 [task-002-my-ecommerce]
🟢 运行中 | 开始时间: 10:00:05 | 持续时间: 15:20
进度: ███████████░░░░░░░░░ 55%
最后活动: 创建 tsconfig.json
```

**协调中心**：
```
活跃代理: 3/5
文件锁定: 2
待处理消息: 0

事件流:
[10:15:23] Agent-1 锁定文件: src/server.ts
[10:15:25] Agent-2 查询接口: UserInterface
[10:15:26] Agent-1 注册接口: UserInterface
[10:15:27] Agent-2 获取接口定义
[10:15:30] Agent-1 释放锁: src/server.ts
[10:15:31] Agent-1 任务完成: task-001
```

#### 第七步：代理协作示例

**场景**：Agent-1创建用户模型，Agent-2需要使用

```python
# Agent-1 的工作流程
# 1. 注册代理
register_agent("my-ecommerce", "task-001-my-ecommerce", 
               "task-001", "task-001-init-structure", 
               "Initialize project structure")

# 2. 创建用户接口
interface_definition = """
interface User {
  id: string;
  email: string;
  password: string;
  createdAt: Date;
}
"""

# 3. 注册接口供其他代理使用
register_interface("my-ecommerce", "task-001-my-ecommerce", 
                   "User", interface_definition)

# 4. 锁定文件
announce_file_change("my-ecommerce", "task-001-my-ecommerce",
                     "src/models/User.ts", "create",
                     "Creating user model")

# 5. 创建文件
create_file("src/models/User.ts", interface_definition)

# 6. 提交代码
git_commit("feat: add user model")

# 7. 释放锁
release_file_lock("my-ecommerce", "task-001-my-ecommerce",
                  "src/models/User.ts")

# 8. 标记完成
mark_task_completed("my-ecommerce", "task-001-my-ecommerce", 
                    "task-001")
```

```python
# Agent-2 的工作流程（同时进行）
# 1. 注册代理
register_agent("my-ecommerce", "task-002-my-ecommerce",
               "task-002", "task-002-user-api",
               "Create user authentication API")

# 2. 需要用户接口定义
user_interface = query_interface("my-ecommerce", "User")

# 如果还未定义，Agent-2可以：
if not user_interface:
    # 选项A：等待
    wait_for_interface("User", timeout=60)
    
    # 选项B：询问其他代理
    query_agent("my-ecommerce", "task-002-my-ecommerce",
                "task-001-my-ecommerce", "interface",
                "Have you defined the User interface?")
    
    # 选项C：先做其他工作
    work_on_different_file()

# 3. 获取到接口后，继续工作
user_interface = query_interface("my-ecommerce", "User")
use_interface_in_api(user_interface)
```

#### 第八步：处理冲突

**文件冲突场景**：

```
情况：Agent-1和Agent-2都需要修改同一个文件

时间线：
10:20:00 - Agent-1尝试锁定 src/config.ts
           成功获取锁
10:20:05 - Agent-2尝试锁定 src/config.ts
           检测到已被Agent-1锁定
10:20:06 - Agent-2的决策：
           选项1: 等待Agent-1释放锁（如果预计很快完成）
           选项2: 询问Agent-1的进度
           选项3: 先处理其他文件
10:20:07 - Agent-2选择选项2:
           query_agent(to="task-001-my-ecommerce",
                       message="How long will you take on config.ts?")
10:20:10 - Agent-1回复:
           "Almost done, about 2 minutes"
10:20:11 - Agent-2选择等待
10:22:00 - Agent-1释放锁
10:22:01 - Agent-2获取锁，继续工作
```

#### 第九步：任务完成和合并

**自动合并流程**（如果启用）：

```
1. Agent完成工作
   ↓
2. 提交代码到分支
   ↓
3. 标记任务COMPLETED
   ↓
4. 进入合并队列
   ↓
5. 合并队列按merge_order排序
   ↓
6. 检查合并前置条件：
   - 没有文件锁冲突
   - 依赖任务已合并
   - 通过自动化测试（如果配置）
   ↓
7. 执行合并：
   - 切换到main分支
   - 合并任务分支
   - 推送到remote（如果配置）
   ↓
8. 清理：
   - 删除worktree
   - （可选）删除分支
   - 更新任务状态为MERGED
   ↓
9. 通知：
   - WebSocket推送合并事件
   - 前端显示成功通知
```

**手动审查流程**（如果禁用自动合并）：

```
1. Agent完成工作
   ↓
2. 任务状态：COMPLETED
   ↓
3. 开发者在UI中查看：
   - 代码diff
   - 修改的文件列表
   - Commit信息
   ↓
4. 开发者决策：
   选项A: 批准合并
   选项B: 请求修改
   选项C: 手动合并
   ↓
5. 如果批准：
   - 点击"Merge"按钮
   - 执行合并流程
```

#### 第十步：项目完成

所有任务完成后：

```
统计信息：
- 总任务数: 10
- 已完成: 10
- 合并: 10
- 总开发时间: 2小时
- 代码提交: 45次
- 修改文件: 78个
- 新增代码: 3,500行
```

**后续操作**：
1. 停止Orchestrator
2. 审查最终代码
3. 运行完整的测试套件
4. 部署到生产环境

---

## 项目结构

### 目录布局

```
splitmind/                           # 项目根目录
│
├── 📄 README.md                     # 项目文档
├── 📄 LICENSE                       # MIT许可证
├── 📄 requirements.txt              # Python依赖
├── 📄 .gitignore                    # Git忽略文件
│
├── 🚀 launch-dashboard.py           # 一键启动脚本
├── ⚙️ setup.py                      # 安装脚本
│
├── 📊 dashboard/                    # Web Dashboard
│   │
│   ├── 🔧 backend/                  # FastAPI后端
│   │   ├── __init__.py
│   │   ├── main.py                  # FastAPI应用入口
│   │   ├── api.py                   # REST API路由
│   │   ├── models.py                # 数据模型
│   │   ├── config.py                # 配置管理器
│   │   ├── project_manager.py       # 项目管理
│   │   ├── task_config.py           # 任务配置
│   │   ├── orchestrator.py          # 编排器
│   │   ├── a2amcp_orchestrator.py   # A2AMCP增强编排器
│   │   ├── merge_queue.py           # 合并队列
│   │   ├── a2amcp_merge_queue.py    # A2AMCP合并队列
│   │   ├── websocket_manager.py     # WebSocket管理
│   │   ├── coordination_monitor.py  # 协调监控
│   │   ├── anthropic_client.py      # Anthropic API客户端
│   │   ├── claude_spawner.py        # Claude代理启动器
│   │   ├── claude_pty_runner.py     # PTY运行器
│   │   └── tmux_viewer.py           # Tmux查看器
│   │
│   └── 🎨 frontend/                 # React前端
│       ├── public/                  # 静态资源
│       ├── src/
│       │   ├── components/          # React组件
│       │   │   ├── ui/              # UI基础组件
│       │   │   │   ├── button.tsx
│       │   │   │   ├── card.tsx
│       │   │   │   ├── dialog.tsx
│       │   │   │   ├── input.tsx
│       │   │   │   ├── tabs.tsx
│       │   │   │   └── ...
│       │   │   │
│       │   │   ├── Logo.tsx
│       │   │   ├── ProjectSelector.tsx
│       │   │   ├── ProjectManager.tsx
│       │   │   ├── CommandCenter.tsx
│       │   │   ├── TaskBoard.tsx
│       │   │   ├── AgentMonitor.tsx
│       │   │   ├── AgentCoordination.tsx
│       │   │   ├── OrchestratorControl.tsx
│       │   │   ├── GlobalSettings.tsx
│       │   │   └── ...
│       │   │
│       │   ├── hooks/               # 自定义Hooks
│       │   │   └── useWebSocket.ts
│       │   │
│       │   ├── services/            # API服务
│       │   │   ├── api.ts
│       │   │   └── websocket.ts
│       │   │
│       │   ├── types/               # TypeScript类型
│       │   │   └── index.ts
│       │   │
│       │   ├── App.tsx              # 主应用组件
│       │   ├── main.tsx             # React入口
│       │   └── index.css            # 全局样式
│       │
│       ├── index.html
│       ├── package.json             # npm依赖
│       ├── tsconfig.json            # TypeScript配置
│       ├── vite.config.ts           # Vite配置
│       └── tailwind.config.js       # Tailwind配置
│
├── 🤖 A2AMCP/                       # 代理协调系统（可选）
│   ├── mcp-server-redis.py          # MCP协调服务器
│   ├── docker-compose.yml           # Docker配置
│   └── sdk/                         # Python SDK
│       └── a2amcp/
│           ├── __init__.py
│           ├── client.py
│           └── models.py
│
├── 📊 projects/                     # 用户项目（运行时创建）
│   └── my-project/
│       ├── .splitmind/              # SplitMind元数据
│       │   ├── config.json          # 项目配置
│       │   └── tasks.json           # 任务数据
│       ├── worktrees/               # Git worktrees
│       │   ├── task-001-branch/
│       │   ├── task-002-branch/
│       │   └── ...
│       └── [项目源代码]
│
├── 📁 scripts/                      # 工具脚本
│   ├── auto-merge.py
│   └── cleanup.sh
│
├── 🧪 testing/                      # 测试文件
│   ├── test_orchestrator.py
│   ├── test_coordination.py
│   └── ...
│
└── 📄 config.json                   # 全局配置（运行时创建）
```

### 关键文件详解

#### 1. `launch-dashboard.py`
**用途**：一键启动脚本
**功能**：
- 检查并安装依赖
- 检测并关闭已运行的实例
- 验证前端构建
- 启动FastAPI服务器
- 自动打开浏览器

**命令行选项**：
```bash
python launch-dashboard.py              # 默认启动
python launch-dashboard.py --port 3000  # 自定义端口
python launch-dashboard.py --dev        # 开发模式（热重载）
python launch-dashboard.py --no-browser # 不自动打开浏览器
```

#### 2. `setup.py`
**用途**：首次安装配置
**功能**：
- 检查系统依赖（Python, Node.js, Git, tmux）
- 安装Python包
- 安装前端依赖并构建
- 创建必要的目录结构
- 检查Claude CLI（可选）

#### 3. `dashboard/backend/api.py`
**用途**：REST API路由定义
**主要端点**：
```python
# 项目管理
GET    /api/projects              # 获取所有项目
POST   /api/projects              # 创建项目
GET    /api/projects/{id}         # 获取项目详情
PUT    /api/projects/{id}         # 更新项目
DELETE /api/projects/{id}         # 删除项目

# 任务管理
GET    /api/projects/{id}/tasks                # 获取任务列表
POST   /api/projects/{id}/tasks                # 创建任务
PUT    /api/projects/{id}/tasks/{task_id}      # 更新任务
DELETE /api/projects/{id}/tasks/{task_id}      # 删除任务

# AI功能
POST   /api/projects/{id}/generate-plan        # AI生成项目计划
POST   /api/projects/{id}/generate-tasks       # AI生成任务

# 代理管理
GET    /api/projects/{id}/agents               # 获取代理列表
POST   /api/agents/{session}/stop              # 停止代理

# 编排器
POST   /api/orchestrator/start                 # 启动编排器
POST   /api/orchestrator/stop                  # 停止编排器
GET    /api/orchestrator/status                # 获取状态
PUT    /api/orchestrator/config                # 更新配置

# 协调
GET    /api/projects/{id}/coordination/stats   # 获取协调统计
GET    /api/projects/{id}/coordination/live    # 实时协调事件流
```

#### 4. `dashboard/backend/orchestrator.py`
**用途**：核心编排逻辑
**主要职责**：
- 任务队列管理
- 代理生命周期管理
- 状态监控
- 自动合并触发

**关键方法**：
```python
async def start(project_id):
    """启动编排器"""
    
async def stop():
    """停止编排器"""
    
async def _orchestrator_loop():
    """主循环"""
    
async def _manage_task_queue():
    """管理任务队列"""
    
async def _spawn_agents():
    """启动代理"""
    
async def _check_agent_status():
    """检查代理状态"""
    
async def _spawn_agent_for_task(task):
    """为任务启动单个代理"""
```

#### 5. `dashboard/frontend/src/App.tsx`
**用途**：前端主应用组件
**功能**：
- 路由和导航
- 全局状态管理
- WebSocket连接
- 模态框管理

#### 6. `config.json`
**用途**：全局配置存储
**结构**：
```json
{
  "anthropic_api_key": "sk-ant-...",
  "orchestrator": {
    "enabled": true,
    "max_concurrent_agents": 5,
    "auto_merge": false,
    "merge_strategy": "merge",
    "auto_spawn_interval": 60,
    "api_provider": "anthropic",
    "api_model": "claude-sonnet-4-20250514"
  },
  "projects": [
    {
      "id": "project-uuid",
      "name": "My Project",
      "path": "/path/to/project",
      "max_agents": 3,
      ...
    }
  ]
}
```

---

## 主要组件

### 前端组件详解

#### 1. ProjectManager组件
**文件**：`dashboard/frontend/src/components/ProjectManager.tsx`

**功能**：
- 显示所有项目的网格视图
- 创建新项目
- 编辑项目配置
- 删除项目（带确认）
- 重置项目

**关键特性**：
```typescript
// 安全删除确认
const [deleteConfirmText, setDeleteConfirmText] = useState('')
const canDelete = deleteConfirmText === project.name

// 完整清理选项
const [completeCleanup, setCompleteCleanup] = useState(false)
```

#### 2. CommandCenter组件
**文件**：`dashboard/frontend/src/components/CommandCenter.tsx`

**功能**：
- 项目概览面板
- 多标签视图（任务/代理/协调/设置）
- 编排器控制
- 实时统计显示

**布局**：
```
┌─────────────────────────────────────────┐
│ 📊 Project Stats  │ 🎛️ Orchestrator    │
├─────────────────────────────────────────┤
│ [Tasks] [Agents] [Coordination] [⚙️]   │
├─────────────────────────────────────────┤
│                                         │
│          Tab Content                    │
│                                         │
└─────────────────────────────────────────┘
```

#### 3. TaskBoard组件
**文件**：`dashboard/frontend/src/components/TaskBoard.tsx`

**功能**：
- 看板式任务展示
- 拖拽排序（可选）
- 任务详情查看和编辑
- 状态筛选

**任务卡片**：
```typescript
interface TaskCard {
  id: string
  title: string
  description: string
  status: TaskStatus
  branch: string
  priority: number
  dependencies: string[]
}
```

#### 4. AgentMonitor组件
**文件**：`dashboard/frontend/src/components/AgentMonitor.tsx`

**功能**：
- 显示所有活跃代理
- 代理状态和进度
- 启动时间和持续时间
- iTerm集成（macOS）
- 手动停止代理

**代理卡片**：
```typescript
interface AgentCard {
  id: string
  session_name: string
  task_id: string
  task_title: string
  status: 'running' | 'completed' | 'failed'
  progress: number
  started_at: Date
  last_activity: Date
}
```

#### 5. AgentCoordination组件
**文件**：`dashboard/frontend/src/components/AgentCoordination.tsx`

**功能**：
- 实时代理网络图
- 协调事件流
- 统计面板
- 文件锁显示

**可视化**：
```typescript
// 代理节点
interface AgentNode {
  id: string
  label: string
  status: 'active' | 'idle' | 'offline'
  progress: number
  todos_completed: number
  todos_total: number
}

// 连接线（通信）
interface AgentEdge {
  from: string
  to: string
  type: 'message' | 'interface_query' | 'file_lock'
}
```

#### 6. GlobalSettings组件
**文件**：`dashboard/frontend/src/components/GlobalSettings.tsx`

**功能**：
- Anthropic API密钥配置
- 编排器参数设置
- 模型选择
- 配置保存和验证

**设置项**：
```typescript
interface GlobalSettings {
  api_key: string
  orchestrator: {
    enabled: boolean
    max_concurrent_agents: number
    auto_merge: boolean
    auto_spawn_interval: number
    merge_strategy: 'merge' | 'squash' | 'rebase'
  }
}
```

### 后端组件详解

#### 1. ProjectManager类
**文件**：`dashboard/backend/project_manager.py`

**职责**：
- 项目CRUD操作
- Git仓库验证
- Worktree管理
- 任务数据持久化

**关键方法**：
```python
class ProjectManager:
    def __init__(self, project_id: str):
        self.project_id = project_id
        self.project = self._load_project()
        self.project_path = Path(self.project.path)
        self.splitmind_dir = self.project_path / ".splitmind"
        self.worktrees_dir = self.project_path / "worktrees"
    
    def create_project(self, data: dict) -> Project:
        """创建新项目"""
        
    def get_tasks(self) -> List[Task]:
        """获取所有任务"""
        
    def create_task(self, task_data: dict) -> Task:
        """创建新任务"""
        
    def update_task(self, task_id: str, updates: dict) -> Task:
        """更新任务"""
        
    def delete_project(self, complete_cleanup: bool = False):
        """删除项目"""
```

#### 2. OrchestratorManager类
**文件**：`dashboard/backend/orchestrator.py`

**职责**：
- 编排器生命周期管理
- 任务调度
- 代理生命周期管理
- 状态监控

**核心逻辑**：
```python
class OrchestratorManager:
    async def _orchestrator_loop(self):
        """主编排循环"""
        while self.running:
            # 1. 管理任务队列
            await self._manage_task_queue()
            
            # 2. 启动代理
            await self._spawn_agents()
            
            # 3. 检查代理状态
            await self._check_agent_status()
            
            # 4. 处理完成的任务
            await self._check_and_merge_completed_tasks()
            
            # 等待下一个周期
            await asyncio.sleep(self.config.auto_spawn_interval)
    
    async def _spawn_agent_for_task(self, task: Task):
        """为任务启动代理"""
        # 1. 创建worktree
        # 2. 启动tmux session
        # 3. 运行Claude Code
        # 4. 注册到A2AMCP
        # 5. 更新任务状态
```

#### 3. WebSocketManager类
**文件**：`dashboard/backend/websocket_manager.py`

**职责**：
- 管理WebSocket连接
- 广播事件
- 处理断开重连

**实现**：
```python
class WebSocketManager:
    def __init__(self):
        self.connections: Set[WebSocket] = set()
    
    async def connect(self, websocket: WebSocket):
        """接受新连接"""
        await websocket.accept()
        self.connections.add(websocket)
    
    async def disconnect(self, websocket: WebSocket):
        """处理断开"""
        self.connections.remove(websocket)
    
    async def broadcast(self, message: WebSocketMessage):
        """广播消息到所有客户端"""
        data = message.json()
        for connection in self.connections:
            try:
                await connection.send_text(data)
            except:
                await self.disconnect(connection)
```

#### 4. CoordinationMonitor类
**文件**：`dashboard/backend/coordination_monitor.py`

**职责**：
- 监控A2AMCP协调活动
- 收集协调统计
- 提供实时事件流

**方法**：
```python
class CoordinationMonitor:
    async def get_coordination_stats(self, project_id: str):
        """获取协调统计"""
        return {
            'active_agents': len(agents),
            'total_todos': todo_count,
            'completed_todos': completed_count,
            'file_locks': len(locks),
            'recent_messages': messages[:50]
        }
    
    async def live_coordination_stream(self, project_id: str):
        """SSE协调事件流"""
        while True:
            events = await self._collect_recent_events()
            yield events
            await asyncio.sleep(1)
```

---

## 创新亮点

### 1. 🌊 波次化任务管理

**创新点**：传统的任务管理是平面的列表，SplitMind引入了波次（Wave）概念。

**优势**：
- **自动依赖解析**：AI自动识别哪些任务必须先完成
- **渐进式构建**：从基础设施到核心功能再到增强特性
- **并行最大化**：同一波次内的任务可以完全并行
- **风险降低**：每个波次完成后可以进行验证

**实现机制**：
```python
# 任务依赖配置
task_config = {
    "task-001": {
        "wave": 1,
        "dependencies": [],
        "priority": 1
    },
    "task-005": {
        "wave": 2,
        "dependencies": ["task-001", "task-002"],
        "priority": 5
    }
}

# 编排器自动识别可执行任务
eligible_tasks = [
    task for task in unclaimed_tasks
    if all(dep.status == MERGED for dep in task.dependencies)
]
```

### 2. 🤖 自主协调的AI代理

**创新点**：代理不是被动执行命令，而是能够主动协调和通信。

**能力**：
- **自我注册**：启动时向协调中心报告
- **主动查询**：需要信息时询问其他代理
- **资源声明**：修改文件前声明意图
- **进度报告**：实时更新工作状态
- **完成通知**：工作完成时主动标记

**示例对话**：
```
Agent-1: "我要创建User接口，请问有人在用这个名字吗？"
Coordination: "没有冲突"
Agent-1: "好的，我注册User接口并开始工作"

Agent-2: "我需要User接口定义"
Coordination: "Agent-1已经注册了，这是定义..."
Agent-2: "收到，我会使用这个定义"

Agent-1: "我要修改config.ts"
Coordination: "已锁定"

Agent-3: "我也需要修改config.ts"
Coordination: "Agent-1正在使用，预计2分钟完成"
Agent-3: "好的，我先做其他工作"
```

### 3. 🧠 AI驱动的项目规划

**创新点**：不需要人工分解任务，AI自动生成完整的开发计划。

**输入最少化**：
```
项目概述：一个电商平台
初始需求：用户认证、产品管理、订单处理、支付集成
技术栈：React + Node.js + PostgreSQL
```

**输出**：
- 完整的任务列表（15-30个任务）
- 详细的任务描述
- 依赖关系图
- 波次分组
- 每个任务的自定义提示词
- 预估时间和优先级

**智能分析**：
- 识别技术栈最佳实践
- 建议架构模式
- 预测潜在风险
- 推荐测试策略

### 4. 🔄 实时可视化协调

**创新点**：将抽象的多代理协调过程可视化为直观的图形界面。

**可视化元素**：

```
实时代理网络图：
        ┌─────┐
        │ A-1 │ ←───── 心跳
        └──┬──┘
           │ 文件锁
           ↓
    [config.ts] 🔒
           ↑
           │ 等待
        ┌──┴──┐
        │ A-2 │ ←───── 查询接口
        └─────┘         ↓
                    回复定义
```

**事件时间轴**：
```
10:23:45 ●───────● 10:24:12 ●───● 10:24:30
         │       │           │   │
     注册Agent  锁定文件    查询  完成
```

### 5. 🛡️ 智能冲突避免

**创新点**：不是事后解决冲突，而是事前预防冲突。

**机制**：

**1. 文件级锁定**：
```python
# Agent-1宣布意图
announce_file_change(
    file="src/Button.tsx",
    action="modify",
    reason="Adding hover effect"
)
# 系统检查：该文件是否已被锁定？
# 如果未锁定 → 授予锁，其他代理看到警告
# 如果已锁定 → 拒绝，提供锁持有者信息
```

**2. 任务级隔离**：
```python
task_config = {
    "exclusive_files": ["src/auth/*.ts"],
    "shared_files": ["src/config.ts"],
    "creates_files": ["src/components/Login.tsx"]
}
```

**3. 依赖感知调度**：
```python
# 只有依赖任务完成后才会启动
if all(dep.status == MERGED for dep in task.dependencies):
    spawn_agent(task)
```

### 6. 📊 企业级项目管理

**创新点**：将AI开发集成到专业的项目管理流程中。

**功能**：
- **多项目支持**：在同一个Dashboard管理多个项目
- **安全删除**：必须输入项目名才能删除，防止误操作
- **完整清理**：删除时可选择清理所有worktrees、分支、session
- **项目重置**：保留配置但清空所有任务和状态
- **审计追踪**：记录所有关键操作
- **统计分析**：项目进度、代理效率、任务完成率

### 7. 🔌 可扩展的架构

**创新点**：模块化设计，易于扩展和定制。

**扩展点**：

**1. API提供商**：
```python
# 当前支持Anthropic Claude
# 可扩展支持：
- OpenAI GPT-4
- Google Gemini
- 本地LLM（Ollama）
- 自定义API
```

**2. 协调协议**：
```python
# 当前：A2AMCP over Redis
# 可扩展：
- MQTT协议
- 分布式系统（Kafka）
- WebSocket直连
- 自定义协议
```

**3. 任务执行器**：
```python
# 当前：Claude Code in tmux
# 可扩展：
- Docker容器
- 远程服务器
- Cloud函数
- GitHub Codespaces
```

---

## 应用场景

### 场景1：快速构建MVP

**客户故事**：
Alex是一个独立开发者，有一个创业想法需要快速验证。

**传统方式**：
- 时间：3-4周全职开发
- 成本：自己的时间或外包费用$5,000-$10,000
- 风险：花费大量时间后发现市场不需要

**使用SplitMind**：
```
第1天：
- 用30分钟写清楚项目需求
- AI Task Master生成开发计划
- 审查并调整计划

第2天：
- 启动Orchestrator
- 5个AI代理并行工作
- 8小时完成基础功能

第3天：
- 继续完善功能
- 修复bug
- 部署到测试环境

第4-5天：
- 用户测试
- 根据反馈调整
- 准备发布

结果：
- 时间：5天从想法到可测试产品
- 成本：AI API费用 ~$50
- 价值：快速验证想法，节省数周时间
```

### 场景2：大型项目并行开发

**客户故事**：
Beta Corp需要为客户定制一个复杂的企业系统，项目包含20+个模块。

**传统方式**：
- 团队：8名开发者
- 时间：3个月
- 问题：代码冲突频繁，集成困难

**使用SplitMind**：
```
规划阶段（1周）：
- 详细需求分析
- AI生成完整任务分解（80个任务）
- 人工审查和优化
- 设置依赖关系和优先级

执行阶段（4周）：
波次1（基础设施 - 1周）：
- 10个AI代理并行工作
- 数据库、认证、API框架
- 人类开发者审查代码

波次2（核心模块 - 2周）：
- 15个AI代理处理主要功能
- 人类开发者处理复杂业务逻辑
- A2AMCP确保模块间接口一致

波次3（集成和优化 - 1周）：
- AI处理集成任务
- 人类开发者性能优化
- 完整测试

结果：
- 时间：5周（提速60%）
- 团队：8名人类开发者 + 15个AI代理
- 质量：更少的代码冲突，更一致的代码风格
```

### 场景3：维护遗留项目

**客户故事**：
Gamma Inc有一个5年前的代码库，需要添加新功能和修复技术债务。

**挑战**：
- 原始开发者已离职
- 文档不完整
- 代码库庞大复杂
- 测试覆盖率低

**使用SplitMind**：
```
准备阶段：
1. 创建项目并导入代码库
2. 使用AI分析项目结构
   提示："分析这个项目的架构，识别主要模块和依赖关系"
3. 生成项目文档

执行阶段：
任务1：添加新功能
- AI代理分析相关代码
- 识别需要修改的文件
- 实现新功能
- 编写单元测试

任务2：重构技术债务
- 多个AI代理并行重构不同模块
- 保持接口兼容
- 增加测试覆盖率

任务3：更新文档
- AI生成代码文档
- 更新README和API文档

结果：
- 不需要深入理解整个代码库
- AI快速定位相关代码
- 并行处理多个改进
- 提升代码质量和可维护性
```

### 场景4：学习和实验

**客户故事**：
Delta是一个学生，想要学习现代Web开发并构建个人项目。

**挑战**：
- 对很多技术还不熟悉
- 不确定最佳实践
- 想要快速看到结果

**使用SplitMind**：
```
学习过程：
1. 创建学习项目
   "构建一个博客系统，使用React、Node.js、MongoDB"

2. AI生成详细计划
   - 包含每个步骤的解释
   - 推荐最佳实践
   - 提供学习资源链接

3. 观察AI代理工作
   - 看AI如何组织代码结构
   - 学习命名规范
   - 理解设计模式

4. 自己实现部分功能
   - 使用AI生成的代码作为参考
   - 尝试修改和扩展
   - AI帮助修复错误

5. 完成项目
   - 获得可工作的项目
   - 深入理解了技术栈
   - 建立了实际经验

价值：
- 学习曲线更平缓
- 快速获得反馈
- 看到最佳实践的实际应用
- 建立自信心
```

### 场景5：定期维护任务

**客户故事**：
Epsilon公司需要定期更新多个项目的依赖、修复安全漏洞、更新文档。

**传统方式**：
- 每个项目需要1-2天手动更新
- 容易遗漏某些项目
- 开发者觉得枯燥

**使用SplitMind**：
```
批量维护流程：

1. 创建维护任务模板
   - 更新package.json依赖
   - 运行安全审计
   - 更新CI/CD配置
   - 刷新文档

2. 应用到所有项目
   for project in projects:
       splitmind.apply_template(project, maintenance_template)

3. 启动批量处理
   - 为每个项目启动代理
   - 并行处理所有项目
   - 自动创建PR

4. 人工审查
   - 查看生成的PR
   - 批准或请求修改
   - 自动合并和部署

结果：
- 时间：从2天/项目 → 2小时全部完成
- 一致性：所有项目同步更新
- 开发者满意度：解放时间做更有意义的工作
```

---

## 竞争优势

### 与传统开发相比

| 方面 | 传统开发 | SplitMind |
|------|----------|------------|
| **速度** | 线性增长 | 并行扩展，5-10倍提速 |
| **成本** | 人力成本高 | AI成本低，可预测 |
| **可扩展性** | 受团队规模限制 | 可启动任意数量代理 |
| **一致性** | 依赖代码审查 | AI遵循统一规范 |
| **24/7可用性** | 需要轮班 | AI不间断工作 |
| **学习曲线** | 新人需要培训 | AI立即可用 |
| **错误率** | 人为错误难免 | AI遵循最佳实践 |

### 与其他AI辅助工具相比

**GitHub Copilot / Cursor**:
- **定位**：单一开发者的编码助手
- **SplitMind优势**：
  - 项目级自动化，不仅仅是代码补全
  - 多代理并行，真正的团队协作
  - 端到端流程，从规划到部署
  - 项目管理集成

**AutoGPT / BabyAGI**:
- **定位**：通用AI代理框架
- **SplitMind优势**：
  - 专注软件开发领域
  - 实用的工程实现（Git、tmux集成）
  - 可视化Dashboard
  - 企业级功能

**Devin (Cognition AI)**:
- **定位**：自主软件工程师
- **SplitMind优势**：
  - 开源且可自部署
  - 多代理并行而非单一代理
  - 更低的成本
  - 可定制和扩展

**LangChain / AutoGen**:
- **定位**：AI代理框架库
- **SplitMind优势**：
  - 开箱即用的完整解决方案
  - 专门为软件开发优化
  - 友好的用户界面
  - 不需要编程配置

### 核心竞争力

#### 1. **垂直整合**
从任务规划到代码合并的完整工作流，不需要额外工具。

#### 2. **真正的并行化**
不是单一AI代理的顺序工作，而是多个代理真正并行协作。

#### 3. **工程化成熟度**
- Git worktree隔离
- tmux会话管理
- WebSocket实时更新
- 企业级安全

#### 4. **协调协议创新**
A2AMCP是专门为多代理软件开发设计的协调协议，解决了实际痛点。

#### 5. **用户体验**
专业的Web Dashboard，不是命令行工具，降低了使用门槛。

#### 6. **成本效益**
使用Claude API的成本远低于雇佣开发者，且可以按需扩展。

---

## 技术栈

### 前端技术

**核心框架**：
- **React 18** - 组件化UI框架
- **TypeScript 5** - 类型安全
- **Vite 5** - 快速构建工具

**UI组件**：
- **Radix UI** - 无样式的可访问组件
- **Tailwind CSS** - 实用优先的CSS框架
- **Framer Motion** - 动画库
- **Lucide React** - 图标库

**状态管理**：
- **TanStack Query (React Query)** - 服务器状态管理
- **React Hooks** - 本地状态管理

**数据可视化**：
- **Recharts** - 图表库
- **自定义SVG** - 协调网络图

**构建工具**：
- **npm** - 包管理
- **PostCSS** - CSS后处理
- **ESLint** - 代码质量
- **TypeScript Compiler** - 类型检查

### 后端技术

**核心框架**：
- **FastAPI** - 现代Python Web框架
- **Python 3.8+** - 编程语言
- **Uvicorn** - ASGI服务器

**数据验证**：
- **Pydantic** - 数据模型和验证

**异步支持**：
- **asyncio** - 异步编程
- **aiofiles** - 异步文件IO

**HTTP客户端**：
- **httpx** - 异步HTTP客户端（用于Anthropic API）

**API集成**：
- **Anthropic SDK** - Claude AI集成

### 数据存储

**配置存储**：
- **JSON文件** - config.json, projects.json
- **文件系统** - 项目元数据

**协调存储**：
- **Redis** - 内存数据库
  - 代理注册表
  - 文件锁
  - 消息队列
  - Todo追踪
  - 接口定义

**版本控制**：
- **Git** - 代码版本管理
- **Git Worktree** - 隔离的工作目录

### 基础设施

**进程管理**：
- **tmux** - 终端复用器
- **psutil** - 进程监控

**容器化**：
- **Docker** - A2AMCP服务容器化
- **Docker Compose** - 服务编排

**AI服务**：
- **Claude Code (CLI)** - Anthropic的命令行工具
- **Anthropic API** - Claude AI模型

### 开发工具

**测试**：
- **pytest** - Python测试框架
- **pytest-asyncio** - 异步测试支持

**代码质量**：
- **Black** - Python代码格式化
- **Ruff** - Python linter
- **ESLint** - JavaScript/TypeScript linter

**类型检查**：
- **mypy** - Python静态类型检查
- **TypeScript** - JavaScript类型系统

### 通信协议

**HTTP/WebSocket**：
- **REST API** - 标准HTTP请求
- **WebSocket** - 实时双向通信
- **Server-Sent Events (SSE)** - 服务器推送

**A2AMCP**：
- **Redis Pub/Sub** - 消息发布订阅
- **Redis Hash** - 键值存储
- **Redis List** - 队列

### 部署环境

**支持的操作系统**：
- **Linux** - 主要支持（Ubuntu, Debian等）
- **macOS** - 完全支持
- **Windows** - 通过WSL2支持

**系统要求**：
- **Python** - 3.8或更高版本
- **Node.js** - 18或更高版本
- **Git** - 2.25或更高版本
- **tmux** - 3.0或更高版本
- **Redis** - 6.0或更高版本（通过Docker）
- **Docker** - 20.10或更高版本（可选，用于A2AMCP）

### 技术架构决策记录

#### 为什么选择FastAPI而不是Flask？
**决策**：FastAPI
**原因**：
- 原生异步支持，处理WebSocket更高效
- 自动API文档生成（OpenAPI/Swagger）
- Pydantic集成，类型安全
- 性能更好（基于Starlette）
- 现代Python特性支持

#### 为什么选择React而不是Vue或Svelte？
**决策**：React + TypeScript
**原因**：
- 最大的生态系统和社区
- 丰富的组件库（Radix UI）
- 成熟的工具链
- TypeScript集成完善
- 招聘和协作更容易

#### 为什么选择Redis而不是数据库？
**决策**：Redis用于协调，JSON文件用于配置
**原因**：
- 协调需要极高的读写速度
- 内存数据库延迟低
- 丰富的数据结构（Hash, List, Set）
- Pub/Sub原生支持
- 项目元数据不需要复杂查询，JSON文件足够

#### 为什么使用Git Worktree？
**决策**：Git Worktree用于代理隔离
**原因**：
- 物理隔离的工作目录
- 避免分支切换开销
- 并行修改不同分支
- Git原生支持，无额外依赖
- 清理简单

#### 为什么使用tmux？
**决策**：tmux管理代理会话
**原因**：
- 持久化终端会话
- 可以attach查看代理工作
- 跨平台支持
- 进程隔离
- 易于管理（kill session）

---

## 总结

SplitMind是一个革命性的AI驱动软件开发平台，它通过以下创新解决了软件开发中的关键问题：

### 核心价值主张

1. **速度** - 5-10倍的开发提速
2. **成本** - 显著降低开发成本
3. **质量** - AI遵循最佳实践，代码一致性高
4. **可扩展** - 按需启动任意数量的AI代理
5. **协作** - 多代理智能协调，避免冲突
6. **易用** - 友好的Web界面，降低门槛

### 适用人群

- 独立开发者和创业者
- 中小型开发团队
- 企业开发部门
- 技术顾问和外包团队
- 开源项目维护者
- 学生和学习者

### 技术亮点

- 🧠 AI驱动的任务规划
- 🤖 多代理并行执行
- 🔗 A2AMCP协调协议
- 📊 实时可视化Dashboard
- 🛡️ 企业级安全管理
- 🔄 完整的开发工作流

### 未来展望

SplitMind v2.0是一个坚实的基础，未来计划包括：
- 支持更多AI模型（OpenAI, Gemini, 本地LLM）
- GitHub深度集成
- 团队协作功能
- 云端部署选项
- 插件系统
- 企业功能（SSO, 审计日志）

---

## 快速开始

准备好体验AI驱动的软件开发了吗？

```bash
# 1. 克隆仓库
git clone https://github.com/webdevtodayjason/splitmind.git
cd splitmind

# 2. 一键安装
python setup.py

# 3. 启动Dashboard
python launch-dashboard.py

# 4. 打开浏览器
# http://localhost:8000
```

开始你的AI协作开发之旅！

---

**文档版本**：2.0
**最后更新**：2025年12月
**作者**：SplitMind Team
**许可证**：MIT License

---

如需更多信息，请访问：
- 📖 GitHub: https://github.com/webdevtodayjason/splitmind
- 📧 Email: jason@webdevtoday.com
- 🌐 Website: https://webdevtoday.com

---

*SplitMind - 让AI代理为你协作开发* 🚀
